# UTF-8
#
# Archivo de información de versión para PyInstaller (VSVersionInfo)
# Este archivo define los metadatos que se incrustan en el ejecutable generado.
# Puedes modificar los valores para actualizar la información del producto.

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 0, 1),         # Versión interna del archivo (mayor, menor, revisión, build)
    prodvers=(1, 0, 0, 1),         # Versión del producto (mayor, menor, revisión, build)
    mask=0x3f,
    flags=0x0,
    OS=0x4,
    fileType=0x1,                  # 0x1 = aplicación ejecutable
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo([
      StringTable(
        '040904B0',  # Español (Latinoamérica) / Unicode
        [
          StringStruct('CompanyName', 'Dev Tools'),
          StringStruct('FileDescription', 'Aplicativo de nombramiento de archivos RIPS CUV XML PDF'),
          StringStruct('FileVersion', '1.0.0.1'),
          StringStruct('InternalName', 'Renombrar Archivos'),     # Nombre interno (coincide con tu script principal)
          StringStruct('LegalCopyright', '© 2025 Dev Tools. Todos los derechos reservados.'),
          StringStruct('OriginalFilename', 'cuv.exe'),  # Nombre de tu EXE final
          StringStruct('ProductName', 'SERAF - Sistema de Renombrado de Archivos de Facturación'),
          StringStruct('ProductVersion', '1.0.0.1')
        ]
      )
    ]),
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)